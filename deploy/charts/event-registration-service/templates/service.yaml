apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-registration-service.fullname" . }}
  namespace: eventreg
  labels:
    app: {{ include "event-registration-service.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "event-registration-service.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http