apiVersion: v1
kind: Service
metadata:
  name: {{ include "participant-service.fullname" . }}
  namespace: eventreg
  labels:
    app: {{ include "participant-service.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "participant-service.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http